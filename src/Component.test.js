import { $, expect } from '@wdio/globals'

describe('Render Components', function(){
    it('render elements to the DOM', () => {
        browser.url('https://www.google.com/')
        
        
    })
})
